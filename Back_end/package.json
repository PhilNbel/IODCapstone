{
  "name": "back_end",
  "version": "1.0.0",
  "description": "",
  "main": "db_run.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local": "set NODE_ENV=dev&& node server.js",
    "aws": "set NODE_ENV=prod&& node server.js",
    "start-local": "concurrently \"cd ../frontend && npm run dev\" \"npm run local\"",
    "start-aws": "concurrently \"cd ../frontend && npm run dev\" \"npm run aws\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.1",
    "sql-sanitizer": "^1.0.0"
  }
}
